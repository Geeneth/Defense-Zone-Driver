---
// Main page for the game
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>DEFENSE ZONE</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      background: #000000;
      margin: 0;
      padding: 0;
      font-family: 'Courier New', monospace;
      color: #ffffff;
      overflow: hidden;
      position: fixed;
      width: 100%;
      height: 100%;
    }

    #game-container {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: #000000;
      image-rendering: pixelated;
      image-rendering: -moz-crisp-edges;
      image-rendering: crisp-edges;
    }

    #game-container canvas {
      display: block;
      image-rendering: pixelated;
      image-rendering: -moz-crisp-edges;
      image-rendering: crisp-edges;
      position: absolute !important;
      top: 50% !important;
      left: 50% !important;
      transform: translate(-50%, -50%) !important;
      max-width: 100%;
      max-height: 100%;
    }

    .title-container {
      position: fixed;
      top: 20px;
      right: 20px;
      z-index: 100;
      pointer-events: none;
    }

    .ascii-title {
      font-family: 'Courier New', monospace;
      font-size: 12px;
      line-height: 1;
      color: #ff6b35;
      text-shadow: 
        0 0 10px #ff6b35,
        0 0 20px #ff6b35;
      white-space: pre;
      font-weight: bold;
      animation: glow 2s ease-in-out infinite alternate;
    }

    @keyframes glow {
      from {
        text-shadow: 
          0 0 10px #ff6b35,
          0 0 20px #ff6b35;
      }
      to {
        text-shadow: 
          0 0 20px #ff6b35,
          0 0 30px #ff6b35,
          0 0 40px #ff8c42;
      }
    }

    .info {
      position: fixed;
      bottom: 20px;
      left: 50%;
      transform: translateX(-50%);
      z-index: 100;
      text-align: center;
      font-size: 0.85rem;
      color: #cccccc;
      text-shadow: 
        -1px -1px 0 #000,
        1px -1px 0 #000,
        -1px 1px 0 #000,
        1px 1px 0 #000;
      max-width: 600px;
      padding: 0 20px;
      font-weight: bold;
      pointer-events: none;
    }

    .retro-scanlines {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      pointer-events: none;
      background: repeating-linear-gradient(
        0deg,
        rgba(0, 0, 0, 0.2),
        rgba(0, 0, 0, 0.2) 1px,
        transparent 1px,
        transparent 2px
      );
      z-index: 1000;
    }

    .loading {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      font-size: 1.5rem;
      font-weight: bold;
      color: #ff6b35;
      text-shadow: 
        -2px -2px 0 #000,
        2px -2px 0 #000,
        -2px 2px 0 #000,
        2px 2px 0 #000,
        0 0 10px #ff6b35;
      animation: pulse 1.5s ease-in-out infinite;
    }

    @keyframes pulse {
      0%, 100% { opacity: 1; }
      50% { opacity: 0.6; }
    }
  </style>
</head>
<body>
  <div class="retro-scanlines"></div>
  
  <div class="title-container">
    <pre class="ascii-title">
██████  ███████ ███████ ███████ ███    ██ ███████ ███████ 
██   ██ ██      ██      ██      ████   ██ ██      ██      
██   ██ █████   █████   █████   ██ ██  ██ ███████ █████   
██   ██ ██      ██      ██      ██  ██ ██      ██ ██      
██████  ███████ ██      ███████ ██   ████ ███████ ███████ 
                                                           
███████  ██████  ███    ██ ███████                        
   ███  ██    ██ ████   ██ ██                             
  ███   ██    ██ ██ ██  ██ █████                          
 ███    ██    ██ ██  ██ ██ ██                             
███████  ██████  ██   ████ ███████                        
    </pre>
  </div>

  <div id="game-container">
    <div class="loading">Loading Game...</div>
  </div>
  
  <div class="info">
    <p>WASD / Arrows: Move • SPACE: Build Tower • R: Ready Up</p>
  </div>

  <script>
    // Determine server URL based on environment
    const isDev = window.location.hostname === 'localhost' || window.location.hostname === '127.0.0.1';
    const SERVER_URL = isDev 
      ? 'http://localhost:3000' 
      : window.location.origin;

    console.log('Connecting to server:', SERVER_URL);

    // Dynamically import the game module (client-side only)
    import('../game/main').then(({ startGame }) => {
      // Remove loading message
      const loading = document.querySelector('.loading');
      if (loading) loading.remove();
      
      // Start the game
      startGame('game-container', SERVER_URL);
    }).catch(error => {
      console.error('Failed to load game:', error);
      const loading = document.querySelector('.loading');
      if (loading) {
        loading.textContent = 'Failed to load game. Check console.';
        loading.style.color = '#ff6b6b';
      }
    });
  </script>
  <script is:inline src="/gesture-input.js"></script>
</body>
</html>
